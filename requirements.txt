patchright>=1.55,<2
beautifulsoup4>=4.12.0
html-to-markdown>=0.1.0
markdownify>=0.11.0
html2text>=2020.1.16
pytest>=7.0
pytest-cov>=6.0

# html-to-markdown requires Rust toolchain (optional).
# Windows users without Rust: comment out the line above; the fallback
# chain (markdownify → html2text → naive regex) handles conversion fine.